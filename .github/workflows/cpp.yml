name: C/C++ CI

on:
  push:
    branches: [ "linux" ]
  pull_request:
    branches: [ "linux" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make Test
      run: make Test
